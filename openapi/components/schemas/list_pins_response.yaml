ListPinsResponse:
  type: object
  properties:
    pins:
      type: array
      items:
        $ref: "../../openapi.yaml#/components/schemas/PinnedMessage"
    nextCursor:
      type: string
      nullable: true
  required: [pins]
