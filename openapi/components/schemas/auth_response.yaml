AuthResponse:
  type: object
  properties:
    accessToken:
      type: string
    refreshToken:
      type: string
    user:
      $ref: "../../openapi.yaml#/components/schemas/User"
  required:
    - accessToken
    - refreshToken
    - user
