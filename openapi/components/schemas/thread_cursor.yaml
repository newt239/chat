ThreadCursor:
  type: object
  properties:
    last_activity_at:
      type: string
      format: date-time
    thread_id:
      type: string
      format: uuid
  required:
    - last_activity_at
    - thread_id

