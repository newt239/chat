MessagesResponse:
  type: object
  properties:
    messages:
      type: array
      items:
        $ref: "../../openapi.yaml#/components/schemas/Message"
    hasMore:
      type: boolean
  required:
    - messages
    - hasMore
