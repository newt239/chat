MessageBookmark:
  type: object
  properties:
    userId:
      type: string
      format: uuid
    messageId:
      type: string
      format: uuid
    createdAt:
      type: string
      format: date-time
  required:
    - userId
    - messageId
    - createdAt

