PinnedMessage:
  type: object
  properties:
    message:
      $ref: "../../openapi.yaml#/components/schemas/Message"
    pinnedAt:
      type: string
      format: date-time
    pinnedBy:
      type: string
      format: uuid
  required: [message, pinnedAt, pinnedBy]
