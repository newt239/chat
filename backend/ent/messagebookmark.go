// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/newt239/chat/ent/message"
	"github.com/newt239/chat/ent/messagebookmark"
	"github.com/newt239/chat/ent/user"
)

// MessageBookmark is the model entity for the MessageBookmark schema.
type MessageBookmark struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MessageBookmarkQuery when eager-loading is set.
	Edges                    MessageBookmarkEdges `json:"edges"`
	message_bookmark_user    *uuid.UUID
	message_bookmark_message *uuid.UUID
	selectValues             sql.SelectValues
}

// MessageBookmarkEdges holds the relations/edges for other nodes in the graph.
type MessageBookmarkEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Message holds the value of the message edge.
	Message *Message `json:"message,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MessageBookmarkEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// MessageOrErr returns the Message value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MessageBookmarkEdges) MessageOrErr() (*Message, error) {
	if e.Message != nil {
		return e.Message, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: message.Label}
	}
	return nil, &NotLoadedError{edge: "message"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MessageBookmark) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case messagebookmark.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case messagebookmark.FieldID:
			values[i] = new(uuid.UUID)
		case messagebookmark.ForeignKeys[0]: // message_bookmark_user
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case messagebookmark.ForeignKeys[1]: // message_bookmark_message
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MessageBookmark fields.
func (_m *MessageBookmark) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case messagebookmark.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case messagebookmark.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case messagebookmark.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field message_bookmark_user", values[i])
			} else if value.Valid {
				_m.message_bookmark_user = new(uuid.UUID)
				*_m.message_bookmark_user = *value.S.(*uuid.UUID)
			}
		case messagebookmark.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field message_bookmark_message", values[i])
			} else if value.Valid {
				_m.message_bookmark_message = new(uuid.UUID)
				*_m.message_bookmark_message = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MessageBookmark.
// This includes values selected through modifiers, order, etc.
func (_m *MessageBookmark) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the MessageBookmark entity.
func (_m *MessageBookmark) QueryUser() *UserQuery {
	return NewMessageBookmarkClient(_m.config).QueryUser(_m)
}

// QueryMessage queries the "message" edge of the MessageBookmark entity.
func (_m *MessageBookmark) QueryMessage() *MessageQuery {
	return NewMessageBookmarkClient(_m.config).QueryMessage(_m)
}

// Update returns a builder for updating this MessageBookmark.
// Note that you need to call MessageBookmark.Unwrap() before calling this method if this MessageBookmark
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *MessageBookmark) Update() *MessageBookmarkUpdateOne {
	return NewMessageBookmarkClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the MessageBookmark entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *MessageBookmark) Unwrap() *MessageBookmark {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: MessageBookmark is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *MessageBookmark) String() string {
	var builder strings.Builder
	builder.WriteString("MessageBookmark(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// MessageBookmarks is a parsable slice of MessageBookmark.
type MessageBookmarks []*MessageBookmark
