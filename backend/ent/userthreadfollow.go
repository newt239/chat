// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/newt239/chat/ent/message"
	"github.com/newt239/chat/ent/user"
	"github.com/newt239/chat/ent/userthreadfollow"
)

// UserThreadFollow is the model entity for the UserThreadFollow schema.
type UserThreadFollow struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserThreadFollowQuery when eager-loading is set.
	Edges                     UserThreadFollowEdges `json:"edges"`
	user_thread_follow_user   *uuid.UUID
	user_thread_follow_thread *uuid.UUID
	selectValues              sql.SelectValues
}

// UserThreadFollowEdges holds the relations/edges for other nodes in the graph.
type UserThreadFollowEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Thread holds the value of the thread edge.
	Thread *Message `json:"thread,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserThreadFollowEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// ThreadOrErr returns the Thread value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserThreadFollowEdges) ThreadOrErr() (*Message, error) {
	if e.Thread != nil {
		return e.Thread, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: message.Label}
	}
	return nil, &NotLoadedError{edge: "thread"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserThreadFollow) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userthreadfollow.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case userthreadfollow.FieldID:
			values[i] = new(uuid.UUID)
		case userthreadfollow.ForeignKeys[0]: // user_thread_follow_user
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case userthreadfollow.ForeignKeys[1]: // user_thread_follow_thread
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserThreadFollow fields.
func (_m *UserThreadFollow) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userthreadfollow.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case userthreadfollow.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case userthreadfollow.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_thread_follow_user", values[i])
			} else if value.Valid {
				_m.user_thread_follow_user = new(uuid.UUID)
				*_m.user_thread_follow_user = *value.S.(*uuid.UUID)
			}
		case userthreadfollow.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_thread_follow_thread", values[i])
			} else if value.Valid {
				_m.user_thread_follow_thread = new(uuid.UUID)
				*_m.user_thread_follow_thread = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserThreadFollow.
// This includes values selected through modifiers, order, etc.
func (_m *UserThreadFollow) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserThreadFollow entity.
func (_m *UserThreadFollow) QueryUser() *UserQuery {
	return NewUserThreadFollowClient(_m.config).QueryUser(_m)
}

// QueryThread queries the "thread" edge of the UserThreadFollow entity.
func (_m *UserThreadFollow) QueryThread() *MessageQuery {
	return NewUserThreadFollowClient(_m.config).QueryThread(_m)
}

// Update returns a builder for updating this UserThreadFollow.
// Note that you need to call UserThreadFollow.Unwrap() before calling this method if this UserThreadFollow
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *UserThreadFollow) Update() *UserThreadFollowUpdateOne {
	return NewUserThreadFollowClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the UserThreadFollow entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *UserThreadFollow) Unwrap() *UserThreadFollow {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserThreadFollow is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *UserThreadFollow) String() string {
	var builder strings.Builder
	builder.WriteString("UserThreadFollow(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserThreadFollows is a parsable slice of UserThreadFollow.
type UserThreadFollows []*UserThreadFollow
