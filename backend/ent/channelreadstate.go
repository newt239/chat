// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/newt239/chat/ent/channel"
	"github.com/newt239/chat/ent/channelreadstate"
	"github.com/newt239/chat/ent/user"
)

// ChannelReadState is the model entity for the ChannelReadState schema.
type ChannelReadState struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// LastReadAt holds the value of the "last_read_at" field.
	LastReadAt time.Time `json:"last_read_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ChannelReadStateQuery when eager-loading is set.
	Edges                      ChannelReadStateEdges `json:"edges"`
	channel_read_state_channel *uuid.UUID
	channel_read_state_user    *uuid.UUID
	selectValues               sql.SelectValues
}

// ChannelReadStateEdges holds the relations/edges for other nodes in the graph.
type ChannelReadStateEdges struct {
	// Channel holds the value of the channel edge.
	Channel *Channel `json:"channel,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ChannelOrErr returns the Channel value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChannelReadStateEdges) ChannelOrErr() (*Channel, error) {
	if e.Channel != nil {
		return e.Channel, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: channel.Label}
	}
	return nil, &NotLoadedError{edge: "channel"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChannelReadStateEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ChannelReadState) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case channelreadstate.FieldLastReadAt:
			values[i] = new(sql.NullTime)
		case channelreadstate.FieldID:
			values[i] = new(uuid.UUID)
		case channelreadstate.ForeignKeys[0]: // channel_read_state_channel
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case channelreadstate.ForeignKeys[1]: // channel_read_state_user
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ChannelReadState fields.
func (_m *ChannelReadState) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case channelreadstate.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case channelreadstate.FieldLastReadAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_read_at", values[i])
			} else if value.Valid {
				_m.LastReadAt = value.Time
			}
		case channelreadstate.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field channel_read_state_channel", values[i])
			} else if value.Valid {
				_m.channel_read_state_channel = new(uuid.UUID)
				*_m.channel_read_state_channel = *value.S.(*uuid.UUID)
			}
		case channelreadstate.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field channel_read_state_user", values[i])
			} else if value.Valid {
				_m.channel_read_state_user = new(uuid.UUID)
				*_m.channel_read_state_user = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ChannelReadState.
// This includes values selected through modifiers, order, etc.
func (_m *ChannelReadState) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryChannel queries the "channel" edge of the ChannelReadState entity.
func (_m *ChannelReadState) QueryChannel() *ChannelQuery {
	return NewChannelReadStateClient(_m.config).QueryChannel(_m)
}

// QueryUser queries the "user" edge of the ChannelReadState entity.
func (_m *ChannelReadState) QueryUser() *UserQuery {
	return NewChannelReadStateClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this ChannelReadState.
// Note that you need to call ChannelReadState.Unwrap() before calling this method if this ChannelReadState
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ChannelReadState) Update() *ChannelReadStateUpdateOne {
	return NewChannelReadStateClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ChannelReadState entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ChannelReadState) Unwrap() *ChannelReadState {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ChannelReadState is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ChannelReadState) String() string {
	var builder strings.Builder
	builder.WriteString("ChannelReadState(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("last_read_at=")
	builder.WriteString(_m.LastReadAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ChannelReadStates is a parsable slice of ChannelReadState.
type ChannelReadStates []*ChannelReadState
