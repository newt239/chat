// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/newt239/chat/ent/channel"
	"github.com/newt239/chat/ent/systemmessage"
	"github.com/newt239/chat/ent/user"
)

// SystemMessage is the model entity for the SystemMessage schema.
type SystemMessage struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Kind holds the value of the "kind" field.
	Kind string `json:"kind,omitempty"`
	// Payload holds the value of the "payload" field.
	Payload map[string]interface{} `json:"payload,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SystemMessageQuery when eager-loading is set.
	Edges                  SystemMessageEdges `json:"edges"`
	system_message_channel *uuid.UUID
	system_message_actor   *uuid.UUID
	selectValues           sql.SelectValues
}

// SystemMessageEdges holds the relations/edges for other nodes in the graph.
type SystemMessageEdges struct {
	// Channel holds the value of the channel edge.
	Channel *Channel `json:"channel,omitempty"`
	// Actor holds the value of the actor edge.
	Actor *User `json:"actor,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ChannelOrErr returns the Channel value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SystemMessageEdges) ChannelOrErr() (*Channel, error) {
	if e.Channel != nil {
		return e.Channel, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: channel.Label}
	}
	return nil, &NotLoadedError{edge: "channel"}
}

// ActorOrErr returns the Actor value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SystemMessageEdges) ActorOrErr() (*User, error) {
	if e.Actor != nil {
		return e.Actor, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "actor"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SystemMessage) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case systemmessage.FieldPayload:
			values[i] = new([]byte)
		case systemmessage.FieldKind:
			values[i] = new(sql.NullString)
		case systemmessage.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case systemmessage.FieldID:
			values[i] = new(uuid.UUID)
		case systemmessage.ForeignKeys[0]: // system_message_channel
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case systemmessage.ForeignKeys[1]: // system_message_actor
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SystemMessage fields.
func (_m *SystemMessage) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case systemmessage.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case systemmessage.FieldKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kind", values[i])
			} else if value.Valid {
				_m.Kind = value.String
			}
		case systemmessage.FieldPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Payload); err != nil {
					return fmt.Errorf("unmarshal field payload: %w", err)
				}
			}
		case systemmessage.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case systemmessage.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field system_message_channel", values[i])
			} else if value.Valid {
				_m.system_message_channel = new(uuid.UUID)
				*_m.system_message_channel = *value.S.(*uuid.UUID)
			}
		case systemmessage.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field system_message_actor", values[i])
			} else if value.Valid {
				_m.system_message_actor = new(uuid.UUID)
				*_m.system_message_actor = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SystemMessage.
// This includes values selected through modifiers, order, etc.
func (_m *SystemMessage) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryChannel queries the "channel" edge of the SystemMessage entity.
func (_m *SystemMessage) QueryChannel() *ChannelQuery {
	return NewSystemMessageClient(_m.config).QueryChannel(_m)
}

// QueryActor queries the "actor" edge of the SystemMessage entity.
func (_m *SystemMessage) QueryActor() *UserQuery {
	return NewSystemMessageClient(_m.config).QueryActor(_m)
}

// Update returns a builder for updating this SystemMessage.
// Note that you need to call SystemMessage.Unwrap() before calling this method if this SystemMessage
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SystemMessage) Update() *SystemMessageUpdateOne {
	return NewSystemMessageClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SystemMessage entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SystemMessage) Unwrap() *SystemMessage {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SystemMessage is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SystemMessage) String() string {
	var builder strings.Builder
	builder.WriteString("SystemMessage(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("kind=")
	builder.WriteString(_m.Kind)
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(fmt.Sprintf("%v", _m.Payload))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SystemMessages is a parsable slice of SystemMessage.
type SystemMessages []*SystemMessage
