# 本ファイルはAnsibleで生成されます

POSTGRES_DB={{ postgres.db }}_preview
POSTGRES_USER={{ postgres.user }}
POSTGRES_PASSWORD={{ lookup('env','POSTGRES_PASSWORD') }}
PREVIEW_DATABASE_URL=postgresql://{{ postgres.user }}:{{ lookup('env','POSTGRES_PASSWORD') }}@db-preview:5432/{{ postgres.db }}_preview?sslmode=disable

PREVIEW_PORT={{ preview_port }}
JWT_SECRET={{ lookup('env','JWT_SECRET') }}
CORS_ALLOWED_ORIGINS={{ cors_allowed_origins }}

WASABI_ACCESS_KEY_ID={{ lookup('env','WASABI_ACCESS_KEY_ID') }}
WASABI_SECRET_ACCESS_KEY={{ lookup('env','WASABI_SECRET_ACCESS_KEY') }}
WASABI_BUCKET_NAME={{ lookup('env','WASABI_BUCKET_NAME') }}
WASABI_REGION={{ lookup('env','WASABI_REGION') }}
WASABI_ENDPOINT={{ lookup('env','WASABI_ENDPOINT') }}

PREVIEW_DOMAIN={{ preview_domain }}
CADDY_EMAIL={{ caddy_email }}
CADDY_HTTP_PORT={{ caddy_http_port }}
CADDY_HTTPS_PORT={{ caddy_https_port }}

BRANCH_NAME={{ sanitized_branch }}
